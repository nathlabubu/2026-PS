# 🎯 Autoavaliação
Conceito pretendido: A

Justificativa (cite a classe/metodo de cada criterio):
- Ex 1 (arrays + metodos): Método `calcularMedia(double[])` implementado em `Exercicio1.java`.
- Ex 2 (arrays + metodos): Método `contarAprovados(double[])` implementado em `Exercicio2.java`.
- Ex 3 (ArrayList): Métodos `adicionarProduto(...)` e `listarProdutos(...)` implementados em `Exercicio3.java`.
- Ex 4 (sobrecarga): Duas assinaturas válidas para `maiorValor` em `Exercicio4.java`, incluindo controle de barreira para arrays vazios.
- Ex 5 (integracao): Método `exibirBoletim(double[])` em `Exercicio5.java` invocando explicitamente `Exercicio1.calcularMedia` e `Exercicio2.contarAprovados`.
- Desafio Nível A: Método complementar `contarAcimaDaMedia(double[])` integrado na saída de dados do `Exercicio5.java`.
